services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3007:3007"
    networks:
      - my_network
    env_file: .env

networks:
  my_network:
    driver: bridge
